{
  "name": "x-safe-worker-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker dashboard for compliant X account publishing and manual single-account actions.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply x_manager_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply x_manager_db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.22.0"
  }
}
